<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NitroAI - Notes Hub</title>
    <style>
        :root {
            /* Furina-inspired Palette */
            --bg-deep: #0D111A;
            --bg-surface: #141B26;
            --bg-surface-hover: #1A2332;
            --border-color: rgba(56, 189, 248, 0.15);
            --border-glow: rgba(56, 189, 248, 0.4);
            
            --hydro-blue: #38BDF8;
            --hydro-deep: #0284C7;
            --theatric-indigo: #1E1B4B;
            --accent-gold: #FACC15;
            
            --text-main: #F8FAFC;
            --text-muted: #94A3B8;
            
            --sidebar-width: 260px;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
        }

        body {
            background-color: var(--bg-deep);
            color: var(--text-main);
            display: flex;
            height: 100vh;
            overflow: hidden;
        }

        /* --- SIDEBAR RESTRUCTURE --- */
        aside {
            width: var(--sidebar-width);
            background-color: var(--bg-surface);
            border-right: 1px solid var(--border-color);
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            padding: 20px;
        }

        .brand {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 30px;
        }

        .brand-logo {
            width: 32px;
            height: 32px;
            background: linear-gradient(135deg, var(--hydro-blue), var(--theatric-indigo));
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            box-shadow: 0 0 12px rgba(56, 189, 248, 0.3);
        }

        .brand-name {
            font-size: 18px;
            font-weight: 700;
            letter-spacing: 0.5px;
        }

        .nav-section {
            display: flex;
            flex-direction: column;
            gap: 6px;
        }

        .nav-label {
            font-size: 11px;
            text-transform: uppercase;
            color: var(--text-muted);
            letter-spacing: 1px;
            margin-bottom: 8px;
        }

        .nav-item {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 10px 14px;
            border-radius: 8px;
            color: var(--text-muted);
            text-decoration: none;
            font-size: 14px;
            transition: all 0.2s ease;
        }

        .nav-item:hover, .nav-item.active {
            background: rgba(56, 189, 248, 0.1);
            color: var(--hydro-blue);
            border-left: 3px solid var(--hydro-blue);
        }

        .user-profile {
            display: flex;
            align-items: center;
            gap: 12px;
            padding-top: 15px;
            border-top: 1px solid var(--border-color);
        }

        .avatar {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--accent-gold), var(--hydro-deep));
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
        }

        /* --- MAIN VIEWPORT (SHIFTED STUFF AROUND) --- */
        main {
            flex: 1;
            display: flex;
            flex-direction: column;
            overflow-y: auto;
            background: radial-gradient(circle at top right, rgba(30, 27, 75, 0.4), transparent 40%),
                        radial-gradient(circle at bottom left, rgba(2, 132, 199, 0.15), transparent 40%);
        }

        /* Top Header Hub */
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 24px 40px;
            border-bottom: 1px solid var(--border-color);
        }

        .search-container {
            position: relative;
            width: 400px;
        }

        .search-container input {
            width: 100%;
            background: var(--bg-surface);
            border: 1px solid var(--border-color);
            padding: 10px 16px 10px 40px;
            border-radius: 20px;
            color: var(--text-main);
            outline: none;
            transition: border-color 0.2s;
        }

        .search-container input:focus {
            border-color: var(--hydro-blue);
            box-shadow: 0 0 8px rgba(56, 189, 248, 0.2);
        }

        .search-icon {
            position: absolute;
            left: 14px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--text-muted);
        }

        /* Content Area */
        .content-body {
            padding: 30px 40px;
            display: flex;
            flex-direction: column;
            gap: 30px;
        }

        .hero-title h1 {
            font-size: 28px;
            font-weight: 700;
            color: var(--text-main);
            margin-bottom: 6px;
        }

        .hero-title p {
            color: var(--text-muted);
            font-size: 14px;
        }

        /* Shifted Quick Cards Grid */
        .grid-cards {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 16px;
        }

        .action-card {
            background: var(--bg-surface);
            border: 1px solid var(--border-color);
            border-radius: 12px;
            padding: 20px;
            display: flex;
            flex-direction: column;
            gap: 12px;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .action-card:hover {
            border-color: var(--hydro-blue);
            transform: translateY(-3px);
            background: var(--bg-surface-hover);
            box-shadow: 0 8px 24px rgba(2, 132, 199, 0.15);
        }

        .card-icon {
            width: 40px;
            height: 40px;
            border-radius: 8px;
            background: rgba(56, 189, 248, 0.1);
            color: var(--hydro-blue);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 18px;
        }

        .action-card h3 {
            font-size: 15px;
            font-weight: 600;
        }

        .action-card p {
            font-size: 12px;
            color: var(--text-muted);
            line-height: 1.4;
        }

        /* Split Workspace Layout (Library & Active Hub) */
        .workspace-split {
            display: grid;
            grid-template-columns: 300px 1fr;
            gap: 24px;
        }

        .panel {
            background: var(--bg-surface);
            border: 1px solid var(--border-color);
            border-radius: 12px;
            padding: 20px;
        }

        .panel-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 16px;
            font-size: 14px;
            font-weight: 600;
        }

        .tree-item {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 8px 10px;
            border-radius: 6px;
            color: var(--text-muted);
            font-size: 13px;
            cursor: pointer;
        }

        .tree-item:hover, .tree-item.active {
            background: rgba(56, 189, 248, 0.1);
            color: var(--hydro-blue);
        }

        .active-document-view {
            display: flex;
            flex-direction: column;
            gap: 16px;
        }

        .doc-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 14px 18px;
            background: rgba(20, 27, 38, 0.6);
            border: 1px solid var(--border-color);
            border-radius: 8px;
            transition: border-color 0.2s;
        }

        .doc-row:hover {
            border-color: var(--hydro-blue);
        }

        .badge {
            background: rgba(56, 189, 248, 0.15);
            color: var(--hydro-blue);
            padding: 4px 10px;
            border-radius: 20px;
            font-size: 11px;
            font-weight: 600;
        }
    </style>
</head>
<body>

    <!-- Sidebar Navigation -->
    <aside>
        <div>
            <div class="brand">
                <div class="brand-logo">N</div>
                <div class="brand-name">NitroAI</div>
            </div>
            
            <div class="nav-section">
                <span class="nav-label">Workspace</span>
                <a href="#" class="nav-item active">📁 Notes Hub</a>
                <a href="#" class="nav-item">📝 Document Editor</a>
                <a href="#" class="nav-item">🎙️ Podcasts</a>
                <a href="#" class="nav-item">⚡ Flashcards</a>
                <a href="#" class="nav-item">❓ Quizzes</a>
            </div>
        </div>

        <div>
            <div class="nav-section" style="margin-bottom: 20px;">
                <a href="#" class="nav-item">⚙️ Settings & Legal</a>
            </div>
            <div class="user-profile">
                <div class="avatar">Y</div>
                <div>
                    <div style="font-size: 13px; font-weight: 600;">You</div>
                    <div style="font-size: 11px; color: var(--text-muted);">Local v1.0.0</div>
                </div>
            </div>
        </div>
    </aside>

    <!-- Main Dynamic Container -->
    <main>
        <header>
            <div class="hero-title" style="margin-bottom: 0;">
                <h1>Notes Hub</h1>
                <p>3 subjects • 3 chapters • 4 notes stored on local storage</p>
            </div>
            <div class="search-container">
                <span class="search-icon">🔍</span>
                <input type="text" placeholder="Search notes and collections... (Ctrl + K)">
            </div>
        </header>

        <div class="content-body">
            
            <!-- Quick Action Cards (Furina Blue Accent Glow) -->
            <div class="grid-cards">
                <div class="action-card">
                    <div class="card-icon">📄</div>
                    <h3>Blank document</h3>
                    <p>Write a Markdown note from scratch.</p>
                </div>
                <div class="action-card">
                    <div class="card-icon">🎙️</div>
                    <h3>Upload audio</h3>
                    <p>Transcribe a lecture or recording locally.</p>
                </div>
                <div class="action-card">
                    <div class="card-icon">📁</div>
                    <h3>Document upload</h3>
                    <p>PDF, DOCX, PPTX, TXT, Markdown.</p>
                </div>
                <div class="action-card">
                    <div class="card-icon">🔗</div>
                    <h3>Website / YouTube</h3>
                    <p>Import article or video captions directly.</p>
                </div>
            </div>

            <!-- Workspace Split: Re-organized Layout -->
            <div class="workspace-split">
                <!-- Library Panel -->
                <div class="panel">
                    <div class="panel-header">
                        <span>Library Tree</span>
                        <span style="color: var(--hydro-blue); cursor: pointer;">+ New</span>
                    </div>
                    <div style="display: flex; flex-direction: column; gap: 4px;">
                        <div class="tree-item">📂 database management...</div>
                        <div class="tree-item" style="padding-left: 20px;">📄 sql (1)</div>
                        <div class="tree-item">📂 database</div>
                        <div class="tree-item" style="padding-left: 20px;">📄 sql (2)</div>
                        <div class="tree-item active">📂 sun tzu</div>
                        <div class="tree-item active" style="padding-left: 20px; background: rgba(56, 189, 248, 0.15); color: var(--hydro-blue);">📄 those (1)</div>
                    </div>
                </div>

                <!-- Active Subject Files View -->
                <div class="panel active-document-view">
                    <div class="panel-header">
                        <span>Active Subject: sun tzu > those</span>
                        <div style="display: flex; gap: 8px;">
                            <span class="badge">Chapter</span>
                            <span style="color: var(--text-muted); font-size: 12px; cursor: pointer; padding: 4px 8px;">Entire subject</span>
                        </div>
                    </div>

                    <div class="doc-row">
                        <div>
                            <div style="font-weight: 600; font-size: 14px; margin-bottom: 4px;">Se common</div>
                            <div style="font-size: 12px; color: var(--text-muted);">document • 4,460 words • updated Sep 11, 2026</div>
                        </div>
                        <div style="display: flex; align-items: center; gap: 12px;">
                            <span class="badge">Semantic index</span>
                            <span style="color: var(--text-muted); cursor: pointer;">•••</span>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </main>

</body>
</html>